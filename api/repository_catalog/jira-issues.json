{
  "id": "atlas.jira-issues",
  "version": "1.1.0",
  "kind": "connector",
  "title": "Jira Issues",
  "author": "Atlas core",
  "description": "Create, read, update, transition, comment on, assign or search Atlassian Jira issues — and look up the accounts to assign them to — through a server-registered instance. The operation and its values are authored in the model; the site URL and the credential live on the server, resolved from the vault (ADR-0201). A transition may be named by the button Jira shows — its id is resolved first — and a search follows Jira's paging to the authored maximum. An account search answers with the accounts themselves, so a process can turn an address into the accountId an assign takes.",
  "engineCompat": ">=0.10",
  "template": {
    "$schema": "https://unpkg.com/@camunda/element-templates-json-schema/resources/schema.json",
    "name": "Jira Issues",
    "id": "atlas.jira-issues",
    "appliesTo": ["bpmn:ServiceTask"],
    "properties": [
      { "label": "Connector", "type": "String", "value": "", "binding": { "type": "property", "name": "atlas:connector" },
        "description": "Name of the server-registered Jira instance (Console → Connectors), never a URL." },
      { "label": "Operation", "type": "Dropdown", "value": "create-issue", "binding": { "type": "property", "name": "atlas:operation" },
        "choices": [
          { "name": "Create issue", "value": "create-issue" },
          { "name": "Read issue", "value": "get-issue" },
          { "name": "Update issue", "value": "update-issue" },
          { "name": "Transition issue", "value": "transition-issue" },
          { "name": "Add comment", "value": "add-comment" },
          { "name": "Assign issue", "value": "assign-issue" },
          { "name": "Search (JQL)", "value": "search" },
          { "name": "Search users", "value": "search-users" }
        ] },
      { "label": "Issue (read/update/transition/comment/assign)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:issueKey" },
        "description": "Issue key (OPS-42) or numeric id. Usually a FEEL expression naming what an earlier Create issue wrote, e.g. =ticket.key." },
      { "label": "Project (create/search users)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:project" },
        "description": "Project key. A value that is all digits is read as a project id instead. On Search users it is optional and restricts the search to the accounts that project can actually assign." },
      { "label": "Issue type (create)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:issueType" },
        "description": "Issue type by the name Jira shows (Task, Bug, Story) — or, if all digits, by its id." },
      { "label": "Summary (create/update)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:summary" } },
      { "label": "Description (create/update)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:description" },
        "description": "Plain text or Jira wiki markup. This connector speaks Jira's v2 API, so a description is a string rather than a document tree." },
      { "label": "Transition (transition)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:transition" },
        "description": "The workflow step by the name Jira shows on the button; its id is resolved first, so the model is not tied to one workflow configuration." },
      { "label": "Comment (comment/transition)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:comment" },
        "description": "Required for Add comment; on a Transition it is optional and is posted in the same call as the move." },
      { "label": "Assignee (assign)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:assignee" },
        "description": "An accountId on Jira Cloud, a username on Data Center. Which one is decided by the connector's credential, not by this value." },
      { "label": "JQL (search)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:jql" },
        "description": "The query, written exactly as in Jira's own search box. It must restrict what it matches: Jira Cloud refuses an unbounded query, so one that only sorts is answered with an HTTP 400 rather than with every issue." },
      { "label": "Search term (search users)", "type": "String", "feel": "optional", "value": "", "binding": { "type": "property", "name": "atlas:query" },
        "description": "A fragment of the account's display name or address, matched as a substring. Jira Cloud searches both; Data Center matches the username. The connector picks the right parameter from its own credential." },
      { "label": "Maximum results (search/search users)", "type": "String", "value": "", "binding": { "type": "property", "name": "atlas:maxResults" },
        "description": "Caps what may reach the result variable; the connector pages up to it. Empty uses 50, 0 reads every match." },
      { "label": "Result variable (create/read/comment/search/search users)", "type": "String", "value": "ticket", "binding": { "type": "property", "name": "atlas:resultVariable" },
        "description": "Receives what Jira returned; a search writes a JSON array. FEEL lists are 1-based, so an account search's first hit is =konten[1].accountId. Update, transition and assign answer with no content, and the compiler refuses a result variable on them." }
    ]
  }
}
