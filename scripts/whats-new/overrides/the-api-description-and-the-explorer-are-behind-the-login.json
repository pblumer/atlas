{
  "tags": [
    "Security"
  ],
  "title": {
    "en": "The API description and its explorer are behind the login",
    "de": "Die API-Beschreibung und ihr Explorer liegen hinter der Anmeldung"
  },
  "summary": {
    "en": "The machine-readable API description and the built-in explorer were readable by anyone who found the port. Nothing on the login screen reads either of them, and the explorer's \"Try it out\" drives the very API a session is required for — so this is the argument `--docs` already makes, one step further. Whether they are served at all is still `--docs`; who reads them is now the login. They moved together on purpose: an explorer that renders and then cannot fetch its own document is worse than one that says plainly it needs a login.",
    "de": "Die maschinenlesbare API-Beschreibung und der eingebaute Explorer waren für jeden lesbar, der den Port fand. Der Anmeldebildschirm liest weder das eine noch das andere, und das „Try it out\" des Explorers bedient genau die API, für die eine Sitzung verlangt wird — das ist also dasselbe Argument, das `--docs` ohnehin schon macht, einen Schritt weiter gedacht. Ob beides überhaupt ausgeliefert wird, entscheidet weiterhin `--docs`; wer es liest, entscheidet jetzt die Anmeldung. Beide sind bewusst gemeinsam umgezogen: Ein Explorer, der sich aufbaut und dann sein eigenes Dokument nicht laden kann, ist schlimmer als einer, der klar sagt, dass er eine Anmeldung braucht."
  }
}
