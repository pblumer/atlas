<?xml version="1.0" encoding="UTF-8"?>
<!--
  Conformance micro-fixture: two independent effects in a straight line.

  Start -> (set_x: = 1 -> x) -> (set_y: = 2 -> y) -> End.

  Metamorphic partner of parallel-independent.bpmn. The two tasks do not depend
  on each other, so serializing them (here) must yield the same effect
  projection as running them concurrently (there).
-->
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:zeebe="http://camunda.org/schema/zeebe/1.0"
             id="defs_linear_independent" targetNamespace="http://atlas/conformance">
  <process id="linear-independent" name="Linear (independent)" isExecutable="true">
    <startEvent id="start"/>
    <scriptTask id="set_x">
      <extensionElements>
        <zeebe:script expression="= 1" resultVariable="x"/>
      </extensionElements>
    </scriptTask>
    <scriptTask id="set_y">
      <extensionElements>
        <zeebe:script expression="= 2" resultVariable="y"/>
      </extensionElements>
    </scriptTask>
    <endEvent id="end"/>
    <sequenceFlow id="f1" sourceRef="start" targetRef="set_x"/>
    <sequenceFlow id="f2" sourceRef="set_x" targetRef="set_y"/>
    <sequenceFlow id="f3" sourceRef="set_y" targetRef="end"/>
  </process>
</definitions>
